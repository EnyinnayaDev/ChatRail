# Google Gemini API key — https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Model to use (multimodal — accepts text + audio)
GEMINI_MODEL=gemini-1.5-flash

# Set True to force the deterministic mock parser (useful for CI / offline dev)
AI_MOCK=False
